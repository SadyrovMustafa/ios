# 🍎 Инструкция для MacBook

## ✅ Код уже на GitHub!

**Репозиторий:** `https://github.com/SadyrovMustafa/ios.git`

---

## 🚀 Быстрый старт (3 шага):

### 1️⃣ Откройте Terminal (или Cursor) и клонируйте проект:

```bash
cd ~/Documents
git clone https://github.com/SadyrovMustafa/ios.git
cd ios
```

**💡 После git clone - открыть папку:**
- 📂 **Как открыть папку в Cursor:** см. `ПОСЛЕ_GIT_CLONE.md` ⭐
- 📥 **Открыть из GitHub:** см. `ОТКРЫТЬ_ИЗ_GITHUB.md`
- 🚀 **Запуск через Cursor:** см. `ЗАПУСК_ЧЕРЕЗ_CURSOR.md`

**Быстрый способ открыть:**
```bash
cursor .  # Если команда работает
# Или через меню: File → Open Folder → ~/Documents/ios
```

### 2️⃣ Откройте iOS проект в Xcode:

**Вариант A (если есть Xcode проект):**
```bash
open -a Xcode .
```

**Вариант B (создать новый проект):**
1. Откройте **Xcode**
2. **File** → **New** → **Project**
3. Выберите **iOS** → **App**
4. Настройки:
   - Product Name: `TickTick`
   - Interface: **SwiftUI**
   - Language: **Swift**
5. Скопируйте файлы из клонированной папки в проект
6. См. `QUICK_START.md` для деталей

### 3️⃣ (Опционально) Запустите web-app:

```bash
cd web-app
npm install
npm start
```

**💡 В Cursor:** Откройте терминал в Cursor и выполните те же команды. Приложение откроется в браузере!

---

## 📦 Что нужно установить:

### Xcode:
- Откройте **App Store**
- Найдите "Xcode" и установите
- Или скачайте с developer.apple.com

### Node.js (для web-app):
```bash
# Через Homebrew
brew install node

# Проверка установки
node --version
npm --version
```

---

## 🔄 Как получать обновления с Windows:

Когда вы делаете изменения на Windows и загружаете их на GitHub, на MacBook выполните:

```bash
cd ~/Documents/ios
git pull
```

---

## 📁 Структура проекта:

```
ios/
├── TickTickApp.swift          # iOS приложение (SwiftUI)
├── Models/                     # Модели данных
├── Views/                      # Экранные представления
├── Managers/                   # Бизнес-логика
└── web-app/                    # Веб-версия (React + TypeScript)
```

---

## ❓ Проблемы?

1. **Xcode не скачивается / требует новую версию macOS:**
   - ⚠️ **См. подробную инструкцию:** `РЕШЕНИЕ_ПРОБЛЕМЫ_XCODE.md`
   - **Быстрое решение:** Работайте с web-app (см. ниже)
   - Установите старую версию Xcode, совместимую с вашей macOS
   - Или обновите macOS (если Mac поддерживает)

2. **Xcode не открывает проект:**
   - Создайте новый проект в Xcode и скопируйте файлы
   - См. `QUICK_START.md`

3. **npm install не работает:**
   - Установите Node.js: `brew install node`
   - Или скачайте с nodejs.org

4. **Git не найден:**
   - Установите Xcode Command Line Tools: `xcode-select --install`

---

## 🚨 Если Xcode не устанавливается - используйте web-app!

**Не нужен Xcode!** Веб-версия работает на любой macOS:

```bash
cd ~/Documents/ios/web-app
npm install
npm start
```

Откройте браузер на `http://localhost:5173` - приложение работает как нативное!

**Можно установить на iPhone как PWA:**
1. Откройте веб-версию в Safari на iPhone
2. Нажмите кнопку "Поделиться" → "На экран Домой"
3. Приложение появится как обычное iOS приложение

---

## 💡 Полезные команды:

```bash
# Проверить статус Git
git status

# Посмотреть изменения
git log

# Отменить локальные изменения
git checkout .

# Обновить проект с GitHub
git pull origin main
```

---

**Готово! Теперь вы можете работать с проектом на MacBook! 🎉**
