# 📧 Настройка Email интеграции

## 🎯 Обзор

TickTick поддерживает отправку email-уведомлений через различные провайдеры:
- **Resend** (рекомендуется) - простой и бесплатный
- **SendGrid** - надежный сервис
- **Mailgun** - хорошая доставляемость
- **SMTP** - через собственный сервер
- **Custom API** - через ваш API endpoint

## 📋 Что отправляется?

- ✅ Приветственное письмо при регистрации
- ✅ Уведомления о назначении задач
- ✅ Напоминания о сроках выполнения
- ✅ Сброс пароля
- ✅ Еженедельные отчеты (опционально)

---

## 🚀 Быстрая настройка (Resend - рекомендуется)

### Шаг 1: Регистрация в Resend

1. Перейдите на [resend.com](https://resend.com)
2. Создайте бесплатный аккаунт
3. Подтвердите email

### Шаг 2: Получение API ключа

1. Перейдите в [API Keys](https://resend.com/api-keys)
2. Нажмите **"Create API Key"**
3. Введите название (например, "TickTick")
4. Скопируйте API ключ (показывается только один раз!)

### Шаг 3: Настройка домена (опционально)

Для использования своего домена:
1. Перейдите в [Domains](https://resend.com/domains)
2. Добавьте свой домен
3. Настройте DNS записи
4. Подождите подтверждения

### Шаг 4: Настройка в TickTick

1. Откройте **"👤 Account"** → **"📧 Настройки Email"**
2. Выберите провайдер: **Resend**
3. Вставьте API ключ
4. Введите email отправителя (можно использовать `noreply@resend.dev` для теста)
5. Нажмите **"Сохранить настройки"**
6. Отправьте тестовое письмо

**Готово!** Теперь при регистрации пользователи будут получать приветственные письма.

---

## 📧 Настройка SendGrid

### Шаг 1: Регистрация

1. Перейдите на [sendgrid.com](https://sendgrid.com)
2. Создайте бесплатный аккаунт (100 писем/день)
3. Подтвердите email

### Шаг 2: Создание API ключа

1. Перейдите в **Settings** → **API Keys**
2. Нажмите **"Create API Key"**
3. Выберите **"Full Access"** или **"Restricted Access"** (только Mail Send)
4. Скопируйте API ключ

### Шаг 3: Настройка в TickTick

1. Выберите провайдер: **SendGrid**
2. Вставьте API ключ
3. Введите email отправителя
4. Сохраните

---

## 📧 Настройка Mailgun

### Шаг 1: Регистрация

1. Перейдите на [mailgun.com](https://mailgun.com)
2. Создайте бесплатный аккаунт (5000 писем/месяц)
3. Подтвердите email

### Шаг 2: Получение API ключа

1. Перейдите в **Sending** → **API Keys**
2. Скопируйте **Private API key**

### Шаг 3: Настройка в TickTick

1. Выберите провайдер: **Mailgun**
2. Вставьте API ключ
3. Введите email отправителя (должен быть на вашем домене Mailgun)
4. Сохраните

---

## 🔧 Настройка SMTP

SMTP требует настройки serverless function или backend API.

### Вариант 1: Vercel Serverless Function

Создайте файл `api/send-email.ts`:

```typescript
import type { VercelRequest, VercelResponse } from '@vercel/node'
import nodemailer from 'nodemailer'

export default async function handler(
  req: VercelRequest,
  res: VercelResponse
) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' })
  }

  const { to, subject, html, text } = req.body

  const transporter = nodemailer.createTransport({
    host: process.env.SMTP_HOST,
    port: parseInt(process.env.SMTP_PORT || '587'),
    secure: false,
    auth: {
      user: process.env.SMTP_USER,
      pass: process.env.SMTP_PASSWORD,
    },
  })

  try {
    await transporter.sendMail({
      from: process.env.SMTP_FROM,
      to,
      subject,
      html,
      text,
    })

    return res.status(200).json({ success: true })
  } catch (error) {
    console.error('SMTP error:', error)
    return res.status(500).json({ error: 'Failed to send email' })
  }
}
```

Установите зависимости:
```bash
npm install nodemailer @types/nodemailer
```

Добавьте переменные окружения в Vercel:
- `SMTP_HOST` - smtp.gmail.com
- `SMTP_PORT` - 587
- `SMTP_USER` - ваш email
- `SMTP_PASSWORD` - пароль приложения
- `SMTP_FROM` - email отправителя

### Вариант 2: Netlify Function

Создайте файл `netlify/functions/send-email.ts` (аналогично Vercel)

---

## 🎨 Кастомизация шаблонов писем

Шаблоны писем находятся в `EmailService.ts`. Вы можете изменить:

- HTML дизайн
- Текст писем
- Стили
- Логотипы

Пример изменения приветственного письма:

```typescript
generateWelcomeEmail(userName: string, userEmail: string): EmailOptions {
  return {
    to: userEmail,
    subject: 'Добро пожаловать в TickTick! 🎉',
    html: `
      <!DOCTYPE html>
      <html>
      <head>
        <style>
          /* Ваши стили */
        </style>
      </head>
      <body>
        <h1>Привет, ${userName}!</h1>
        <!-- Ваш контент -->
      </body>
      </html>
    `
  }
}
```

---

## 🔒 Безопасность

### ⚠️ Важно:

1. **Не храните API ключи в коде**
   - Используйте переменные окружения
   - В продакшене храните на сервере

2. **Не отправляйте API ключи в localStorage**
   - В текущей реализации ключи сохраняются локально (только для демо)
   - В продакшене используйте backend для хранения ключей

3. **Используйте HTTPS**
   - Всегда используйте HTTPS для отправки email
   - Защитите API endpoints

4. **Ограничьте доступ**
   - Используйте ограниченные API ключи (если возможно)
   - Регулярно ротируйте ключи

---

## 🐛 Решение проблем

### Проблема: Письма не отправляются

**Проверьте:**
1. ✅ Правильность API ключа
2. ✅ Email отправителя подтвержден у провайдера
3. ✅ Нет лимитов на отправку (бесплатные тарифы)
4. ✅ Проверьте консоль браузера на ошибки

### Проблема: Письма попадают в спам

**Решения:**
1. Настройте SPF/DKIM записи для домена
2. Используйте подтвержденный домен
3. Избегайте спам-слов в теме и тексте
4. Используйте репутабельные провайдеры (Resend, SendGrid)

### Проблема: Ошибка CORS

**Решение:**
- Email API провайдеры поддерживают CORS
- Если используете Custom API, настройте CORS на сервере

---

## 📊 Мониторинг

### Resend
- Перейдите в [Dashboard](https://resend.com/emails) для просмотра статистики

### SendGrid
- Перейдите в **Activity** для просмотра отправленных писем

### Mailgun
- Перейдите в **Logs** для просмотра истории отправки

---

## 💡 Рекомендации

1. **Начните с Resend** - самый простой вариант
2. **Используйте свой домен** - для лучшей доставляемости
3. **Тестируйте перед продакшеном** - отправляйте тестовые письма
4. **Мониторьте статистику** - отслеживайте доставляемость
5. **Настройте SPF/DKIM** - для защиты от спама

---

**Готово!** Теперь ваше приложение будет отправлять email-уведомления! 🎉

