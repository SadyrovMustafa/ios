# 🚀 Руководство по деплою

## 📋 Подготовка к деплою

### 1. Сборка проекта

```bash
cd web-app
npm install
npm run build
```

Собранные файлы будут в папке `dist/`

### 2. Проверка сборки

```bash
npm run preview
```

Откройте `http://localhost:4173` для проверки

---

## 🌐 Деплой на Vercel

### Способ 1: Через Vercel CLI

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в аккаунт:
```bash
vercel login
```

3. Деплой:
```bash
cd web-app
vercel
```

4. Следуйте инструкциям:
   - Выберите проект
   - Подтвердите настройки
   - Дождитесь завершения

5. Приложение будет доступно по адресу: `https://your-project.vercel.app`

### Способ 2: Через GitHub

1. Загрузите код в GitHub репозиторий

2. Перейдите на [vercel.com](https://vercel.com)

3. Нажмите **"New Project"**

4. Подключите GitHub репозиторий

5. Настройки:
   - **Framework Preset:** Vite
   - **Root Directory:** `web-app`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`

6. Нажмите **"Deploy"**

7. Vercel автоматически задеплоит приложение

### Настройка домена

1. В настройках проекта Vercel
2. Перейдите в **"Domains"**
3. Добавьте свой домен
4. Настройте DNS записи

---

## 🌐 Деплой на Netlify

### Способ 1: Через Netlify CLI

1. Установите Netlify CLI:
```bash
npm i -g netlify-cli
```

2. Войдите в аккаунт:
```bash
netlify login
```

3. Инициализируйте проект:
```bash
cd web-app
netlify init
```

4. Деплой:
```bash
netlify deploy --prod
```

### Способ 2: Через GitHub

1. Загрузите код в GitHub репозиторий

2. Перейдите на [netlify.com](https://netlify.com)

3. Нажмите **"New site from Git"**

4. Подключите GitHub репозиторий

5. Настройки:
   - **Base directory:** `web-app`
   - **Build command:** `npm run build`
   - **Publish directory:** `web-app/dist`

6. Нажмите **"Deploy site"**

### Настройка домена

1. В настройках сайта Netlify
2. Перейдите в **"Domain settings"**
3. Добавьте свой домен
4. Настройте DNS записи

---

## 📦 Деплой на GitHub Pages

### Автоматический деплой (рекомендуется)

1. Загрузите код в GitHub репозиторий

2. Файл `.github/workflows/deploy.yml` уже настроен

3. Перейдите в **Settings** → **Actions** → **General**

4. Включите **"Workflow permissions"** → **"Read and write permissions"**

5. Сделайте commit в ветку `main`:
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

6. GitHub Actions автоматически задеплоит приложение

7. Приложение будет доступно по адресу: `https://your-username.github.io/your-repo/`

### Ручной деплой

1. Соберите проект:
```bash
cd web-app
npm run build
```

2. Перейдите в настройки репозитория → **Pages**

3. Выберите источник: **"GitHub Actions"**

4. Или используйте ветку `gh-pages`:
```bash
git checkout -b gh-pages
git add dist/
git commit -m "Deploy"
git push origin gh-pages
```

### Настройка base path

Если репозиторий не в корне, обновите `vite.config.ts`:

```typescript
export default defineConfig({
  base: '/your-repo-name/',
  // ... остальные настройки
})
```

---

## 🔧 Настройка переменных окружения

### Для Vercel

1. В настройках проекта → **Environment Variables**
2. Добавьте переменные:
   - `VITE_API_URL` - URL API (если есть)
   - `VITE_FIREBASE_API_KEY` - Firebase ключ (если используется)

### Для Netlify

1. В настройках сайта → **Environment variables**
2. Добавьте переменные (аналогично Vercel)

---

## ✅ Проверка после деплоя

1. ✅ Откройте приложение в браузере
2. ✅ Проверьте работу всех функций
3. ✅ Проверьте PWA установку
4. ✅ Проверьте офлайн работу
5. ✅ Проверьте на мобильных устройствах

---

## 🔄 Автоматический деплой

### Vercel и Netlify

Автоматически деплоят при каждом push в `main` ветку.

### GitHub Pages

Использует GitHub Actions (файл уже настроен).

---

## 🐛 Решение проблем

### Проблема: Белая страница после деплоя

**Решение:**
- Проверьте, что все пути начинаются с `/`
- Проверьте настройки base path в `vite.config.ts`
- Проверьте, что `index.html` в корне `dist/`

### Проблема: 404 ошибки при обновлении страницы

**Решение:**
- Убедитесь, что настроены redirects (в `vercel.json` или `netlify.toml`)
- Все маршруты должны перенаправляться на `index.html`

### Проблема: PWA не работает

**Решение:**
- Проверьте, что `manifest.json` доступен
- Проверьте, что Service Worker зарегистрирован
- Убедитесь, что используется HTTPS

### Проблема: Медленная загрузка

**Решение:**
- Включите сжатие (gzip/brotli)
- Используйте CDN
- Оптимизируйте изображения
- Включите кеширование

---

## 📊 Мониторинг

### Vercel Analytics

1. Включите Analytics в настройках проекта
2. Просматривайте метрики производительности

### Netlify Analytics

1. Включите Analytics в настройках сайта
2. Просматривайте статистику посещений

---

## 🔐 Безопасность

1. ✅ Используйте HTTPS (включено по умолчанию)
2. ✅ Не храните секреты в коде
3. ✅ Используйте переменные окружения
4. ✅ Регулярно обновляйте зависимости

---

**Готово!** Ваше приложение задеплоено и доступно в интернете! 🎉

