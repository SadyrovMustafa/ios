# 🔌 REST API - Реализация

## ✅ Что реализовано

### 1. API Service ✅
- **Файл:** `src/services/APIService.ts`
- **Функции:**
  - ✅ Управление API ключами
  - ✅ Аутентификация через API ключи
  - ✅ Rate limiting (1000 запросов/час)
  - ✅ Права доступа (permissions)
  - ✅ Обработка всех endpoints

### 2. API Router ✅
- **Файл:** `src/services/APIRouter.ts`
- **Функции:**
  - ✅ Обработка HTTP запросов
  - ✅ Создание API клиента
  - ✅ Удобные методы (get, post, put, patch, delete)

### 3. API Management Panel ✅
- **Файл:** `src/components/APIManagementPanel.tsx`
- **Функции:**
  - ✅ Создание API ключей
  - ✅ Управление ключами
  - ✅ Выбор прав доступа
  - ✅ Просмотр использования

### 4. API Client ✅
- **Файл:** `src/utils/apiClient.ts`
- **Функции:**
  - ✅ Упрощенный клиент для использования в коде
  - ✅ Все методы для работы с API

### 5. API Endpoint Handler ✅
- **Файл:** `src/utils/apiEndpointHandler.ts`
- **Функции:**
  - ✅ Перехват fetch запросов к `/api/v1/*`
  - ✅ Обработка в браузере (для development)

---

## 📋 Endpoints

### ✅ Реализованные endpoints:

1. **Health Check**
   - `GET /health` или `GET /`

2. **Tasks**
   - `GET /tasks` - получить все задачи
   - `GET /tasks/:id` - получить задачу
   - `POST /tasks` - создать задачу
   - `PATCH /tasks/:id` - обновить задачу
   - `DELETE /tasks/:id` - удалить задачу

3. **Lists**
   - `GET /lists` - получить все списки
   - `GET /lists/:id` - получить список
   - `POST /lists` - создать список
   - `PATCH /lists/:id` - обновить список
   - `DELETE /lists/:id` - удалить список

4. **Projects**
   - `GET /projects` - получить все проекты
   - `GET /projects/:id` - получить проект
   - `POST /projects` - создать проект

5. **Users**
   - `GET /users` - получить всех пользователей
   - `GET /users/:id` - получить пользователя

---

## 🔐 Аутентификация

### API Keys
- Генерация уникальных ключей
- Формат: `tt_timestamp_randomstring`
- Хранение в localStorage
- Права доступа на уровне ключа

### Rate Limiting
- По умолчанию: 1000 запросов/час
- Настраивается для каждого ключа
- Отслеживание в localStorage

---

## 🚀 Использование

### В коде приложения:

```typescript
import { APIClient } from './utils/apiClient'

const api = new APIClient('tt_ваш_ключ')

// Получить задачи
const tasks = await api.getTasks({ completed: 'false' })

// Создать задачу
const newTask = await api.createTask({
  title: 'Новая задача',
  priority: 'high'
})
```

### Через HTTP:

```javascript
fetch('/api/v1/tasks', {
  headers: {
    'X-API-Key': 'tt_ваш_ключ'
  }
})
  .then(res => res.json())
  .then(tasks => console.log(tasks))
```

---

## 📝 Примечания

### Текущая реализация:
- ✅ Работает с localStorage (локальное хранение)
- ✅ API interceptor для development режима
- ✅ Полная документация и примеры

### Для production:
- ⚠️ Нужен бэкенд сервер для обработки HTTP запросов
- ⚠️ Нужна база данных для хранения API ключей
- ⚠️ Нужна настройка CORS для внешних запросов

### Рекомендации:
1. Используйте Node.js/Express для бэкенда
2. Подключите базу данных (PostgreSQL/MongoDB)
3. Настройте HTTPS
4. Добавьте логирование запросов
5. Настройте мониторинг

---

## 🔄 Миграция на бэкенд

### Шаги:

1. **Создать бэкенд сервер** (Node.js/Express)
2. **Перенести APIService** на сервер
3. **Настроить базу данных** для API ключей
4. **Добавить middleware** для аутентификации
5. **Настроить CORS** для внешних запросов
6. **Добавить логирование** и мониторинг

### Пример структуры бэкенда:

```
backend/
├── routes/
│   ├── tasks.js
│   ├── lists.js
│   ├── projects.js
│   └── users.js
├── middleware/
│   ├── auth.js
│   └── rateLimit.js
├── services/
│   └── apiService.js
└── models/
    └── apiKey.js
```

---

## 📚 Документация

- **Полная документация:** [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)
- **Примеры:** [API_EXAMPLES.md](./API_EXAMPLES.md)
- **Быстрый старт:** [API_QUICK_START.md](./API_QUICK_START.md)

---

**Готово!** REST API полностью реализован и готов к использованию! 🎉

