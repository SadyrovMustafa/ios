# 🚀 Запуск TickTick Web App на Windows

## Пошаговая инструкция

### Шаг 1: Установите Node.js

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Выберите LTS версию (рекомендуется)
3. Установите, следуя инструкциям установщика
4. Проверьте установку, откройте PowerShell и выполните:
   ```powershell
   node --version
   npm --version
   ```
   Должны отобразиться версии.

### Шаг 2: Откройте проект

**Вариант A: Используйте готовый скрипт (проще всего!)**
1. Откройте папку `web-app` в проводнике
2. Дважды кликните на файл `start.bat`
3. Готово! Приложение запустится автоматически

**Вариант B: Через командную строку**
1. Откройте PowerShell или Command Prompt
2. **ВАЖНО:** Перейдите в папку `web-app`:
   ```powershell
   cd C:\Users\Mustafa\Documents\ios\web-app
   ```
   ⚠️ Не забудьте `\web-app` в конце пути!

### Шаг 3: Установите зависимости

```powershell
npm install
```

Это займет 1-2 минуты. Дождитесь завершения.

### Шаг 4: Запустите приложение

```powershell
npm run dev
```

Вы увидите что-то вроде:
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### Шаг 5: Откройте в браузере

1. Приложение автоматически откроется в браузере
2. Или откройте вручную: `http://localhost:3000`

## 📱 Установка на iPhone

### Способ 1: Через локальную сеть

1. Узнайте IP адрес вашего компьютера:
   ```powershell
   ipconfig
   ```
   Найдите "IPv4 Address" (например, 192.168.1.100)

2. Запустите приложение с доступом из сети:
   Остановите текущий сервер (Ctrl+C) и запустите:
   ```powershell
   npm run dev -- --host
   ```

3. На iPhone откройте Safari
4. Введите адрес: `http://ВАШ_IP:3000` (например, `http://192.168.1.100:3000`)
5. Убедитесь, что iPhone и компьютер в одной Wi-Fi сети

6. Добавьте на главный экран:
   - Нажмите кнопку "Поделиться" (квадрат со стрелкой)
   - Прокрутите вниз
   - Выберите "На экран Домой"
   - Нажмите "Добавить"

### Способ 2: Через хостинг (рекомендуется)

1. Соберите приложение:
   ```powershell
   npm run build
   ```

2. Загрузите папку `dist` на бесплатный хостинг:
   - **Vercel**: https://vercel.com (самый простой)
   - **Netlify**: https://netlify.com
   - **GitHub Pages**: https://pages.github.com

3. Откройте ссылку на iPhone и добавьте на главный экран

## 🎯 Быстрые команды

```powershell
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Запуск с доступом из сети (для iPhone)
npm run dev -- --host

# Сборка для продакшена
npm run build

# Просмотр собранной версии
npm run preview
```

## ❌ Решение проблем

### Ошибка: "npm не является внутренней или внешней командой"
- **Решение**: Переустановите Node.js и перезапустите PowerShell

### Ошибка: "EACCES: permission denied"
- **Решение**: Запустите PowerShell от имени администратора

### Порт 3000 занят
- **Решение**: Измените порт в `vite.config.ts`:
  ```typescript
  server: {
    port: 3001, // или другой свободный порт
  }
  ```

### iPhone не может подключиться
- Убедитесь, что оба устройства в одной Wi-Fi сети
- Проверьте, что брандмауэр Windows не блокирует подключения
- Попробуйте отключить антивирус временно

## ✅ Готово!

Теперь у вас есть работающее приложение TickTick, которое:
- ✅ Работает на Windows
- ✅ Можно установить на iPhone
- ✅ Сохраняет данные локально
- ✅ Работает офлайн

**Наслаждайтесь!** 🎉

