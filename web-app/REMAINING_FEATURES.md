# 🚧 Оставшиеся функции - Базовая структура

## 📋 Статус: Базовая структура создана

Для оставшихся 4 функций создана базовая архитектура и сервисы. Они готовы к интеграции в UI.

## 6. Вложения файлов и изображений

### ✅ Создано:
- **Файл:** `src/services/FileStorageService.ts`
- **Функции:**
  - Сохранение файлов в IndexedDB
  - Получение файлов для задачи
  - Удаление файлов
  - Конвертация в base64
  - Форматирование размера файла

### ⚠️ Что нужно добавить:
- UI компонент для загрузки файлов
- Отображение вложений в TaskItem и TaskDetailModal
- Поле `attachments` в модель Task
- Интеграция с формами создания/редактирования задач

### 📝 Пример использования:
```typescript
// Сохранение файла
const attachment = await FileStorageService.saveFile(file, taskId)

// Получение файлов
const files = await FileStorageService.getFilesForTask(taskId)

// Удаление файла
await FileStorageService.deleteFile(fileId)
```

## 7. Автоматизация с правилами

### ✅ Создано:
- **Файл:** `src/services/AutomationService.ts`
- **Функции:**
  - Создание правил "если-то"
  - Условия: теги, приоритет, список, дата, название, подзадачи
  - Действия: добавить тег, установить приоритет, переместить, напомнить, добавить подзадачу
  - Применение правил к задачам
  - Сохранение правил

### ⚠️ Что нужно добавить:
- UI для создания и редактирования правил
- Список правил в настройках
- Автоматическое применение при создании/обновлении задач

### 📝 Пример использования:
```typescript
// Создание правила
const rule = AutomationService.addRule({
  name: 'Auto-tag high priority',
  enabled: true,
  conditions: [
    { type: 'priority', operator: 'equals', value: 'high' }
  ],
  actions: [
    { type: 'addTag', value: 'urgent' }
  ]
})

// Применение к задаче
const updatedTask = AutomationService.applyRules(task)
```

## 8. Виджеты для быстрого доступа

### ⚠️ Ограничения PWA:
- PWA не поддерживает нативные виджеты iOS/Android
- Можно использовать только установку на главный экран
- Для виджетов нужна нативная версия приложения

### ✅ Что можно сделать:
- Улучшить PWA манифест для лучшей установки
- Добавить быстрые действия через Share API
- Создать отдельную страницу для виджетов (для будущей нативной версии)

### 📝 Альтернативы:
- **Web Share API** - быстрый доступ через системное меню
- **Shortcuts** - ярлыки на главном экране
- **Badge API** - счетчик на иконке (частично поддерживается)

## 10. Локальная синхронизация (подготовка к облаку)

### ✅ Создано:
- **Файл:** `src/services/SyncService.ts`
- **Функции:**
  - Отслеживание изменений
  - Версионирование данных
  - Уникальный ID устройства
  - Подготовка данных для синхронизации
  - Применение изменений из облака

### ⚠️ Что нужно добавить:
- Интеграция с TaskManager для отслеживания изменений
- UI для управления синхронизацией
- Подключение к облачному API (Firebase, Supabase, etc.)

### 📝 Пример использования:
```typescript
// Добавление изменения
SyncService.addChange({
  type: 'create',
  entityType: 'task',
  entityId: task.id,
  data: task
})

// Подготовка для синхронизации
const syncData = SyncService.prepareForCloudSync()

// Применение изменений
const { tasks, lists } = SyncService.applyCloudChanges(changes)
```

## 🎯 План интеграции

### Приоритет 1: Вложения файлов
1. Добавить поле `attachments: string[]` в Task
2. Создать компонент `FileAttachment.tsx`
3. Интегрировать в AddTaskModal и TaskDetailModal
4. Обновить TaskItem для показа вложений

### Приоритет 2: Автоматизация
1. Создать компонент `AutomationRules.tsx`
2. Добавить страницу настроек
3. Интегрировать применение правил в TaskManager
4. Добавить примеры правил

### Приоритет 3: Синхронизация
1. Интегрировать отслеживание изменений
2. Создать UI для статуса синхронизации
3. Подготовить структуру для облачного API

### Приоритет 4: Виджеты
1. Улучшить PWA манифест
2. Добавить Web Share API
3. Создать документацию для нативной версии

## 📊 Итог

- **Структура готова:** 4/4 функций
- **Готово к интеграции:** Все сервисы созданы
- **Требуется UI:** Компоненты для пользовательского интерфейса
- **Требуется тестирование:** После интеграции

Все базовые сервисы созданы и готовы к использованию! Нужно только добавить UI компоненты и интегрировать их в приложение.

