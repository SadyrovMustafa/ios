# 📂 Как открыть папку после git clone на MacBook

## ✅ После выполнения `git clone`:

```bash
cd ~/Documents
git clone https://github.com/SadyrovMustafa/ios.git
```

Проект будет в папке: `~/Documents/ios`

---

## 🚀 Открыть в Cursor (3 способа):

### Способ 1: Через команду в Terminal (самый быстрый) ⚡

```bash
cd ~/Documents/ios
cursor .
```

Если команда `cursor` не работает, используйте способ 2 или 3.

---

### Способ 2: Через меню Cursor

1. Запустите **Cursor**
2. Нажмите **File** → **Open Folder...** (или `Cmd + O`)
3. В открывшемся окне:
   - Нажмите `Cmd + Shift + G` (или кнопку "Go to Folder")
   - Введите: `~/Documents/ios`
   - Нажмите **Go**
4. Выберите папку `ios` и нажмите **Open**

**Или проще:**
- Перейдите в `Documents` → найдите папку `ios` → выберите → **Open**

---

### Способ 3: Перетащить папку в Cursor

1. Откройте **Finder** (`Cmd + Space` → "Finder")
2. Перейдите в `Documents` → найдите папку `ios`
3. Перетащите папку `ios` на иконку **Cursor** в Dock
4. Cursor откроется с проектом

---

## 📁 Открыть в Finder (чтобы увидеть файлы):

```bash
cd ~/Documents/ios
open .
```

Или вручную:
- Откройте **Finder**
- Перейдите в `Documents` → `ios`

---

## 🎯 Полный путь к папке:

```
/Users/ваше_имя_пользователя/Documents/ios
```

**Как узнать ваше имя пользователя:**
```bash
whoami
```

Тогда полный путь будет:
```
/Users/$(whoami)/Documents/ios
```

---

## ✅ Проверка, что папка открыта правильно:

После открытия в Cursor вы должны увидеть в боковой панели:

```
📁 ios
  📁 .git
  📁 Managers
  📁 Models
  📁 Views
  📁 web-app
  📄 README.md
  📄 Info.plist
  ... (другие файлы)
```

---

## 🔧 Если команда `cursor` не работает:

### Установить Cursor в PATH:

1. Откройте Terminal
2. Выполните:
   ```bash
   # Создать символическую ссылку
   sudo ln -s /Applications/Cursor.app/Contents/Resources/app/bin/cursor /usr/local/bin/cursor
   ```

3. Перезапустите Terminal
4. Проверьте: `cursor --version`

### Или используйте полный путь:

```bash
/Applications/Cursor.app/Contents/MacOS/Cursor ~/Documents/ios
```

---

## 💡 Полезные команды после git clone:

```bash
# Перейти в папку проекта
cd ~/Documents/ios

# Посмотреть содержимое
ls -la

# Проверить статус Git
git status

# Открыть в Cursor
cursor .

# Открыть в Finder
open .

# Открыть в Xcode (если нужно)
open -a Xcode .
```

---

## 🚀 Быстрый старт после открытия:

### 1. Откройте терминал в Cursor:
- Нажмите `` Ctrl + ` `` (или `Terminal` → `New Terminal`)

### 2. Запустите web-app:
```bash
cd web-app
npm install
npm start
```

### 3. Откройте браузер:
- Перейдите на `http://localhost:5173`

---

## 📍 Где находится папка:

После `git clone` папка будет здесь:

**Короткий путь:**
```
~/Documents/ios
```

**Полный путь:**
```
/Users/ваше_имя/Documents/ios
```

**Как найти в Finder:**
1. Откройте Finder
2. Нажмите `Cmd + Shift + H` (перейти в Home)
3. Откройте `Documents` → `ios`

---

## ❓ Проблемы?

### "cursor: command not found"
**Решение:** Используйте способ 2 или 3 (через меню или перетаскивание)

### Папка не найдена
**Решение:** Проверьте, что вы в правильной папке:
```bash
pwd  # Покажет текущую папку
ls   # Покажет содержимое
```

### Cursor не открывается
**Решение:**
- Убедитесь, что Cursor установлен
- Попробуйте открыть через `Applications` → `Cursor`

---

## 🎨 Альтернатива: Открыть через VS Code (если Cursor не работает):

```bash
cd ~/Documents/ios
code .
```

---

## 📋 Чек-лист:

- [ ] Выполнен `git clone`
- [ ] Папка находится в `~/Documents/ios`
- [ ] Cursor установлен
- [ ] Папка открыта в Cursor
- [ ] Видны файлы проекта в боковой панели
- [ ] Терминал открыт (`` Ctrl + ` ``)

---

**Готово! Папка открыта в Cursor! 🎉**
