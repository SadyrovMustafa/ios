# Инструкция по запуску TickTick приложения

## Шаг 1: Создание проекта в Xcode

1. **Откройте Xcode** (если не установлен, скачайте из App Store)

2. **Создайте новый проект:**
   - Нажмите `File` → `New` → `Project...` (или `Cmd + Shift + N`)
   - Выберите **iOS** → **App**
   - Нажмите **Next**

3. **Настройте проект:**
   - **Product Name:** `TickTick`
   - **Team:** Выберите свою команду (или оставьте None)
   - **Organization Identifier:** например, `com.yourname`
   - **Interface:** выберите **SwiftUI**
   - **Language:** выберите **Swift**
   - **Storage:** выберите **None** (мы используем UserDefaults)
   - Нажмите **Next**

4. **Выберите место сохранения:**
   - Выберите папку (можно выбрать текущую папку `ios`)
   - **НЕ** ставьте галочку "Create Git repository" (если не нужен)
   - Нажмите **Create**

## Шаг 2: Добавление файлов в проект

После создания проекта Xcode автоматически создаст файл `TickTickApp.swift` и `ContentView.swift`. 

### Вариант A: Замена существующих файлов

1. **Замените содержимое `TickTickApp.swift`:**
   - Откройте файл `TickTickApp.swift` в Xcode
   - Скопируйте содержимое из нашего файла `TickTickApp.swift`
   - Вставьте и замените содержимое

2. **Удалите старый `ContentView.swift`** и добавьте наши файлы:
   - Правой кнопкой на `ContentView.swift` → `Delete` → `Move to Trash`
   - Правой кнопкой на папку проекта → `New Group` → назовите `Views`
   - Правой кнопкой на папку проекта → `New Group` → назовите `Models`
   - Правой кнопкой на папку проекта → `New Group` → назовите `Managers`

3. **Добавьте файлы:**
   - Правой кнопкой на группу `Models` → `Add Files to "TickTick"...`
   - Выберите файл `Models/Task.swift`
   - Повторите для всех файлов из папок `Views` и `Managers`
   - **Важно:** Убедитесь, что стоит галочка "Copy items if needed"

### Вариант B: Копирование файлов напрямую

1. **Скопируйте файлы в папку проекта:**
   - Откройте папку проекта в Finder (правой кнопкой на проект → `Show in Finder`)
   - Скопируйте все файлы из текущей папки `ios` в папку проекта Xcode
   - Сохраните структуру папок (Models, Views, Managers)

2. **Добавьте файлы в Xcode:**
   - В Xcode правой кнопкой на проект → `Add Files to "TickTick"...`
   - Выберите все папки (Models, Views, Managers) и файлы
   - Убедитесь, что стоит галочка "Copy items if needed"
   - Нажмите **Add**

## Шаг 3: Настройка Info.plist

1. Если в проекте уже есть `Info.plist`, замените его содержимое нашим файлом
2. Или просто убедитесь, что в настройках проекта указан правильный Bundle Identifier

## Шаг 4: Запуск приложения

1. **Выберите симулятор или устройство:**
   - В верхней панели Xcode выберите симулятор (например, "iPhone 15 Pro")
   - Или подключите реальное устройство через USB

2. **Запустите приложение:**
   - Нажмите кнопку **▶️ Play** (или `Cmd + R`)
   - Или выберите `Product` → `Run`

3. **Дождитесь компиляции:**
   - Xcode скомпилирует проект (это может занять несколько секунд)
   - Приложение автоматически откроется в симуляторе или на устройстве

## Возможные проблемы и решения

### Ошибка: "Cannot find type 'Task' in scope"
- **Решение:** Убедитесь, что все файлы добавлены в проект и находятся в правильных группах

### Ошибка: "Missing argument for parameter 'listId'"
- **Решение:** Проверьте, что все файлы из папки Views добавлены в проект

### Ошибка компиляции SwiftUI
- **Решение:** Убедитесь, что выбран iOS 15.0 или выше в настройках проекта:
  - Выберите проект в навигаторе
  - Выберите Target "TickTick"
  - Вкладка "General" → "Minimum Deployments" → установите iOS 15.0

### Приложение не запускается
- **Решение:** 
  - Проверьте, что выбран правильный симулятор/устройство
  - Очистите проект: `Product` → `Clean Build Folder` (`Cmd + Shift + K`)
  - Перезапустите Xcode

## Быстрая проверка структуры проекта

Убедитесь, что структура проекта выглядит так:

```
TickTick (проект)
├── TickTickApp.swift
├── Models
│   └── Task.swift
├── Managers
│   └── TaskManager.swift
├── Views
│   ├── ContentView.swift
│   ├── TaskListView.swift
│   ├── AddTaskView.swift
│   ├── TaskDetailView.swift
│   └── AddListView.swift
└── Info.plist
```

## Готово! 🎉

После успешного запуска вы увидите приложение TickTick с боковой панелью списков и возможностью создания задач.

