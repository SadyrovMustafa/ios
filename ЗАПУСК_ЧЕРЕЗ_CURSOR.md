# 🚀 Запуск iOS приложения через Cursor на macOS

## ⚠️ Важно понимать:

**Cursor** - это редактор кода (как VS Code), он **не может напрямую запускать iOS приложения**. Но через Cursor можно:

✅ Редактировать код  
✅ Использовать встроенный терминал  
✅ Запускать команды сборки  
✅ Запускать web-app  

❌ Но для **сборки и запуска iOS приложения** все равно нужен **Xcode** (хотя бы установленный)

---

## 🎯 Вариант 1: Запуск через терминал Cursor (если есть Xcode проект)

### Если у вас уже есть `.xcodeproj` файл:

1. **Откройте проект в Cursor:**
   - `File` → `Open Folder` → выберите папку проекта

2. **Откройте терминал в Cursor:**
   - `Terminal` → `New Terminal` (или `` Ctrl + ` ``)

3. **Соберите и запустите через командную строку:**

```bash
# Список доступных симуляторов
xcrun simctl list devices

# Собрать проект
xcodebuild -project TickTick.xcodeproj -scheme TickTick -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15 Pro' build

# Запустить на симуляторе
xcodebuild -project TickTick.xcodeproj -scheme TickTick -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15 Pro' test
```

4. **Или запустить симулятор и установить приложение:**

```bash
# Запустить симулятор
open -a Simulator

# Установить приложение (после сборки)
xcrun simctl install booted /path/to/app.app
xcrun simctl launch booted com.yourcompany.TickTick
```

---

## 🎯 Вариант 2: Создать Xcode проект и работать в Cursor

### Шаг 1: Создайте проект в Xcode (один раз)

1. Откройте **Xcode**
2. **File** → **New** → **Project**
3. Выберите **iOS** → **App**
4. Настройки:
   - Product Name: `TickTick`
   - Interface: **SwiftUI**
   - Language: **Swift**
5. Сохраните проект

### Шаг 2: Скопируйте файлы

Скопируйте все файлы из вашего репозитория в папку проекта Xcode.

### Шаг 3: Работайте в Cursor

1. Откройте папку проекта в Cursor
2. Редактируйте код в Cursor
3. Для запуска используйте терминал (команды выше) или откройте в Xcode

---

## 🎯 Вариант 3: Запустить web-app через Cursor (БЕЗ Xcode!)

**Это самый простой способ!** Не нужен Xcode:

### В терминале Cursor:

```bash
cd web-app
npm install
npm start
```

Приложение откроется в браузере на `http://localhost:5173`

**Преимущества:**
- ✅ Работает без Xcode
- ✅ Можно тестировать на iPhone через браузер
- ✅ Все функции iOS версии доступны
- ✅ Можно установить как PWA на iPhone

---

## 🎯 Вариант 4: Использовать Swift Package Manager (если проект поддерживает)

Если проект настроен как Swift Package:

```bash
# Собрать
swift build

# Запустить тесты
swift test
```

Но для iOS приложения все равно нужен Xcode для симулятора.

---

## 📋 Настройка Cursor для работы с Swift

### Установите расширения:

1. Откройте расширения в Cursor: `Cmd + Shift + X`
2. Установите:
   - **Swift** (официальное расширение)
   - **CodeLLDB** (для отладки)
   - **GitLens** (для работы с Git)

### Настройка терминала:

Cursor использует встроенный терминал macOS, поэтому все команды работают как обычно.

---

## 🔧 Полезные команды для терминала Cursor:

### Проверка установки Xcode:

```bash
# Версия Xcode
xcodebuild -version

# Путь к Xcode
xcode-select -p

# Установить Command Line Tools (если нужно)
xcode-select --install
```

### Работа с симулятором:

```bash
# Список симуляторов
xcrun simctl list devices

# Запустить симулятор
open -a Simulator

# Запустить конкретный симулятор
xcrun simctl boot "iPhone 15 Pro"

# Закрыть симулятор
xcrun simctl shutdown all
```

### Сборка iOS приложения:

```bash
# Если есть .xcodeproj
xcodebuild -project TickTick.xcodeproj -scheme TickTick -sdk iphonesimulator build

# Если есть .xcworkspace
xcodebuild -workspace TickTick.xcworkspace -scheme TickTick -sdk iphonesimulator build
```

---

## 🎨 Рекомендуемый workflow:

### Для разработки:

1. **Редактируйте код в Cursor** (удобный редактор)
2. **Тестируйте через web-app** (быстро, без Xcode)
3. **Периодически проверяйте в Xcode** (для финальной сборки iOS)

### Команды в терминале Cursor:

```bash
# Работа с кодом
cd ~/Documents/ios
git pull  # Получить обновления

# Запустить web-app для тестирования
cd web-app
npm start

# Когда готово - открыть в Xcode для финальной сборки
open -a Xcode ../TickTick.xcodeproj
```

---

## ❓ Частые вопросы:

**Q: Можно ли полностью обойтись без Xcode?**  
A: Для веб-версии - да! Для нативного iOS приложения - нет, нужен Xcode для симулятора.

**Q: Можно ли отлаживать iOS приложение в Cursor?**  
A: Частично. Можно использовать `lldb` через терминал, но удобнее отладка в Xcode.

**Q: Как быстро переключаться между Cursor и Xcode?**  
A: Используйте `Cmd + Tab` или запускайте Xcode через терминал: `open -a Xcode .`

**Q: Можно ли использовать Cursor на Windows для iOS разработки?**  
A: Нет, iOS разработка возможна только на macOS. Но можно редактировать код и работать с web-app.

---

## 💡 Рекомендация:

**Используйте комбинацию:**

1. **Cursor** - для редактирования кода (удобный интерфейс, AI-помощник)
2. **Web-app** - для быстрого тестирования (запуск через терминал Cursor)
3. **Xcode** - для финальной сборки и тестирования на симуляторе

Это самый эффективный workflow! 🚀

---

## 🚀 Быстрый старт прямо сейчас:

```bash
# В терминале Cursor на MacBook
cd ~/Documents/ios/web-app
npm install
npm start
```

Откройте браузер - приложение работает! 🎉
