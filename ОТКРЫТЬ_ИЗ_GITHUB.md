# 📥 Открыть проект из GitHub в Cursor на MacBook

## 🚀 Быстрый способ (3 команды):

### 1️⃣ Откройте Terminal на MacBook

Нажмите `Cmd + Space` и введите "Terminal", или найдите в `Applications` → `Utilities` → `Terminal`

### 2️⃣ Клонируйте репозиторий:

```bash
cd ~/Documents
git clone https://github.com/SadyrovMustafa/ios.git
```

### 3️⃣ Откройте в Cursor:

**Способ A: Через команду:**
```bash
cd ios
cursor .
```

**Способ B: Через меню Cursor:**
1. Запустите **Cursor**
2. `File` → `Open Folder...` (или `Cmd + O`)
3. Перейдите в `~/Documents/ios`
4. Нажмите **Open**

---

## 📋 Пошаговая инструкция:

### Шаг 1: Откройте Terminal

- Нажмите `Cmd + Space` (Spotlight)
- Введите "Terminal"
- Нажмите Enter

### Шаг 2: Перейдите в папку Documents

```bash
cd ~/Documents
```

### Шаг 3: Клонируйте репозиторий

```bash
git clone https://github.com/SadyrovMustafa/ios.git
```

Вы увидите:
```
Cloning into 'ios'...
remote: Enumerating objects: ...
Receiving objects: 100% ...
```

### Шаг 4: Откройте в Cursor

**Вариант A: Через команду (самый быстрый):**
```bash
cd ios
cursor .
```

**Вариант B: Через Finder:**
1. Откройте Finder (`Cmd + Space` → "Finder")
2. Перейдите в `Documents` → `ios`
3. Перетащите папку на иконку Cursor в Dock

**Вариант C: Через меню Cursor:**
1. Запустите Cursor
2. `File` → `Open Folder...` (`Cmd + O`)
3. Выберите `~/Documents/ios`
4. Нажмите **Open**

---

## ✅ Проверка:

После открытия в Cursor вы должны увидеть в боковой панели:

```
ios/
├── .gitignore
├── Info.plist
├── Managers/
│   └── TaskManager.swift
├── Models/
│   └── Task.swift
├── Views/
│   ├── ContentView.swift
│   ├── TaskListView.swift
│   └── ...
├── web-app/
│   ├── src/
│   ├── package.json
│   └── ...
├── README.md
└── ...
```

---

## 🎯 Что делать дальше:

### Запустить web-app (без Xcode):

1. Откройте терминал в Cursor: `` Ctrl + ` `` (или `Terminal` → `New Terminal`)
2. Выполните:
   ```bash
   cd web-app
   npm install
   npm start
   ```
3. Откройте браузер: `http://localhost:5173`

### Работать с iOS кодом:

- Редактируйте файлы `.swift` в Cursor
- Для запуска нужен Xcode (см. `ЗАПУСК_ЧЕРЕЗ_CURSOR.md`)

---

## 🔄 Обновление проекта:

Когда вы делаете изменения на Windows и загружаете на GitHub:

```bash
# В терминале Cursor
cd ~/Documents/ios
git pull
```

---

## 📍 Полный путь:

После клонирования проект будет здесь:
```
/Users/ваше_имя_пользователя/Documents/ios
```

Или короткий путь:
```
~/Documents/ios
```

---

## ❓ Проблемы?

### "git: command not found"
**Решение:**
```bash
xcode-select --install
```

### "cursor: command not found"
**Решение:**
1. Установите Cursor с [cursor.sh](https://cursor.sh)
2. Или используйте способ B/C (через меню или Finder)

### "Permission denied"
**Решение:**
```bash
# Проверьте права доступа
ls -la ~/Documents

# Если нужно, дайте права
chmod 755 ~/Documents
```

### Репозиторий не клонируется
**Решение:**
- Проверьте интернет-соединение
- Убедитесь, что репозиторий публичный или у вас есть доступ
- Попробуйте еще раз: `git clone https://github.com/SadyrovMustafa/ios.git`

---

## 💡 Полезные команды:

```bash
# Проверить, что репозиторий клонирован
ls ~/Documents/ios

# Перейти в папку проекта
cd ~/Documents/ios

# Проверить статус Git
git status

# Посмотреть все файлы
ls -la

# Открыть в Cursor
cursor .

# Открыть в Finder
open .
```

---

## 🎨 Альтернативный способ (через GitHub Desktop):

Если установлен GitHub Desktop:

1. Откройте **GitHub Desktop**
2. `File` → `Clone Repository`
3. Найдите `SadyrovMustafa/ios`
4. Выберите папку для клонирования
5. Нажмите **Clone**
6. `Repository` → `Open in External Editor` → выберите Cursor

---

## 🚀 Одна команда для всего:

Если Cursor установлен и добавлен в PATH:

```bash
cd ~/Documents && git clone https://github.com/SadyrovMustafa/ios.git && cd ios && cursor .
```

Эта команда:
1. Перейдет в Documents
2. Клонирует репозиторий
3. Перейдет в папку проекта
4. Откроет в Cursor

---

**Готово! Проект открыт в Cursor! 🎉**
