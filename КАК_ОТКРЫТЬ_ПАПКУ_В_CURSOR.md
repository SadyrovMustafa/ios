# 📂 Как открыть папку проекта в Cursor на MacBook

## 🚀 Быстрый способ (3 шага):

### 1️⃣ Клонируйте проект (если еще не сделано):

Откройте **Terminal** на MacBook и выполните:

```bash
cd ~/Documents
git clone https://github.com/SadyrovMustafa/ios.git
cd ios
```

### 2️⃣ Откройте папку в Cursor:

**Способ A: Через меню Cursor:**
1. Запустите **Cursor**
2. Нажмите **File** → **Open Folder...** (или `Cmd + O`)
3. Перейдите в `~/Documents/ios`
4. Нажмите **Open**

**Способ B: Через терминал:**
```bash
cd ~/Documents/ios
cursor .
```

**Способ C: Перетащить папку:**
1. Откройте **Finder**
2. Перейдите в `~/Documents/ios`
3. Перетащите папку на иконку Cursor в Dock

**Способ D: Через контекстное меню:**
1. В **Finder** найдите папку `ios`
2. Правой кнопкой → **Open With** → **Cursor**

---

## ✅ Проверка, что все открылось правильно:

После открытия вы должны увидеть в боковой панели Cursor:

```
ios/
├── .gitignore
├── Info.plist
├── Managers/
├── Models/
├── Views/
├── web-app/
├── README.md
└── ... (другие файлы)
```

---

## 🎯 Что делать дальше:

### Если хотите запустить web-app:

1. Откройте терминал в Cursor: `Terminal` → `New Terminal` (или `` Ctrl + ` ``)
2. Выполните:
   ```bash
   cd web-app
   npm install
   npm start
   ```

### Если хотите работать с iOS кодом:

- Редактируйте файлы `.swift` в Cursor
- Для запуска нужен Xcode (см. `ЗАПУСК_ЧЕРЕЗ_CURSOR.md`)

---

## 🔧 Настройка Cursor для работы с проектом:

### Установите полезные расширения:

1. Откройте расширения: `Cmd + Shift + X`
2. Установите:
   - **Swift** (для подсветки синтаксиса Swift)
   - **GitLens** (для работы с Git)
   - **ES7+ React/Redux/React-Native snippets** (для web-app)

### Настройте терминал:

Cursor использует встроенный терминал macOS. Все команды работают как обычно.

---

## 📍 Полный путь к папке:

```
/Users/ваше_имя_пользователя/Documents/ios
```

Или короткий путь:
```
~/Documents/ios
```

---

## ❓ Проблемы?

**Папка не открывается:**
- Убедитесь, что Cursor установлен
- Проверьте, что папка существует: `ls ~/Documents/ios`

**Не видно файлов:**
- Проверьте, что вы открыли правильную папку (не `web-app`, а корневую `ios`)
- Обновите вид: `View` → `Refresh`

**Терминал не работает:**
- Перезапустите Cursor
- Проверьте настройки терминала: `Settings` → `Terminal`

---

## 💡 Полезные горячие клавиши:

- `Cmd + O` - Открыть папку
- `Cmd + K, Cmd + O` - Открыть папку (альтернатива)
- `` Ctrl + ` `` - Открыть/закрыть терминал
- `Cmd + P` - Быстрый поиск файлов
- `Cmd + Shift + P` - Командная палитра

---

**Готово! Теперь вы можете работать с проектом в Cursor! 🎉**
